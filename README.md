# Lotus

This repository contains a simple Flask application implementing user registration, login and email confirmation.

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
```

Run the application:

```bash
python app.py
```

During registration a confirmation link is printed to the console. Visit the link to confirm the account before logging in.

## Running tests

Execute the tests with `pytest`:

```bash
pytest
```
